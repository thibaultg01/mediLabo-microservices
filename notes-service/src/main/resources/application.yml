server:
  port: 8082

spring:
  application:
    name: notes-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/notesdb
  
app:
  auth:
    username: ${APP_AUTH_USERNAME:user}
    password: ${APP_AUTH_PASSWORD:password}